version: '3.8'
services:
  detect:
    build:
      context: ./detect
      dockerfile: Dockerfile
    ports:
      - "8000:80"
    volumes:
      - ./shared:/app/shared
      - ./shared/static/uploads:/app/static/uploads
    environment:
      - PYTHONPATH=/app
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
  feedback:
    build:
      context: ./feedback
      dockerfile: Dockerfile
    ports:
      - "8001:80"
    volumes:
      - ./shared:/app/shared
      - ./shared/static/uploads:/app/static/uploads
    environment:
      - PYTHONPATH=/app