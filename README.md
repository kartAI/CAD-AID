# Test on local host with Docker
To run  CAD-AID backend API using swagger on local host in Docker, execute following steps:

```sh
docker run -p 8080:80 ghcr.io/kartai/cadaid
```

Go to 
```
http://localhost:8080/docs
```
